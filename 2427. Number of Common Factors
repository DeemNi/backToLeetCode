/**
 * @param {number} a
 * @param {number} b
 * @return {number}
 */
var commonFactors = function(a, b) {
    let n = a > b ? b : a;
    let counter = 0;

    for(let i = 1; i <= n; i++){
        if(a % i == 0 && b % i == 0){
            counter++;
        }
    }
    return counter;
};
