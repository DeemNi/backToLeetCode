/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var map = function (arr, fn) {
    let i = 0;
    let returnedArray = [];

    while (i < arr.length) {
        returnedArray[i] = fn(arr[i], i)
        i++;
    }
    return returnedArray;
};
